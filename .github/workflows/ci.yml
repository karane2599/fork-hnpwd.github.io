name: Continuous Integration Checks
on: pull_request
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get install sbcl
      - run: sbcl --script test.lisp
      - run: sbcl --script gen.lisp --validate-only
